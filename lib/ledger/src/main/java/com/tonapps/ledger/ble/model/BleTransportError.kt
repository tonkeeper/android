package com.tonapps.ledger.ble.model

/*
import com.ledger.live.core.TransportError

sealed class BleTransportError constructor(private val description: String): TransportError {
    override fun message() {
        L.e("BleTransportError", description)
    }

    class ConnectError(description: String):BleTransportError(description)
    class WriteError(description: String):BleTransportError(description)
    class ReadError(description: String):BleTransportError(description)
    class ListenError(description: String):BleTransportError(description)
    class ScanError(description: String):BleTransportError(description)
    //class LowerLevelError(description: String)
}
*/
/**
 * AUTO-GENERATED FILE - DO NOT EDIT
 *
 * This file was automatically generated by the Tonapi OpenAPI Generator.
 * Generator: OpenAPI Generator (https://openapi-generator.tech)
 *
 * To regenerate, run: ./tools/scripts/generators/tonapi/update-all.sh
 * Manual changes will be overwritten on next generation.
 */

package io.exchangeapi.models

import kotlinx.serialization.Serializable
import kotlinx.serialization.SerialName
import kotlinx.serialization.Contextual

@Serializable
data class CrossSwapRoute(
    @SerialName(value = "route_id")
    val routeId: String,

    @Contextual @SerialName(value = "aggregator")
    val aggregator: CrossSwapAggregator,

    @Contextual @SerialName(value = "route_type")
    val routeType: CrossSwapRouteType,

    /* Raw base-units amount the user is expected to receive. For `exact_output` it equals the requested `destination_amount`.  */
    @SerialName(value = "estimated_destination_amount")
    val estimatedDestinationAmount: String,
    @SerialName(value = "minimum_destination_amount")
    val minimumDestinationAmount: String,
    @SerialName(value = "legs")
    val legs: List<CrossSwapLeg>,

    @Contextual @SerialName(value = "date_expire")
    val dateExpire: java.time.OffsetDateTime,

    @Contextual @SerialName(value = "risk_level")
    val riskLevel: CrossSwapRiskLevel,

    /* The aggregator's own identifier for this route, passed through verbatim: swapsxyz `txId`, swapkit `routeId`, omniston `quoteId`. Interpret it together with `aggregator`. Omitted when the provider does not expose one.  */
    @SerialName(value = "provider_route_id")
    val providerRouteId: String? = null,

    /* Underlying protocol or DEX that actually executes the swap (provider-driven, free-form). */
    @SerialName(value = "protocol")
    val protocol: String? = null,

    /* Raw base-units amount the user is paying. For `exact_input` it is echoed back from the quote request; for `exact_output` it is the provider's expected input, exclusive of slippage.  */
    @SerialName(value = "source_amount")
    val sourceAmount: String? = null,

    /* Raw base-units cap on what the user may pay, inclusive of slippage. Present only for `exact_output` quotes.  */
    @SerialName(value = "maximum_source_amount")
    val maximumSourceAmount: String? = null,
    @SerialName(value = "estimated_time")
    val estimatedTime: CrossSwapTimeEstimate? = null,

    /* Aggregate slippage across all legs (bps) */
    @SerialName(value = "total_slippage_bps")
    val totalSlippageBps: Int? = null,

    /* USD value difference between input and output, in basis points (1% = 100 bps, so -300 = -3%). Negative means the user receives less USD than they pay. Omitted when |bps| < 300 or USD prices are unavailable.  */
    @SerialName(value = "value_difference_bps")
    val valueDifferenceBps: Int? = null,

    /* Provider-estimated price impact of the trade, in basis points (1% = 100 bps). Higher = more slippage vs mid-market. Omitted when the provider cannot estimate it (e.g. HTLC orders, low liquidity paths).  */
    @SerialName(value = "price_impact_bps")
    val priceImpactBps: Int? = null,

    /* USD price of one whole source-asset unit at quote time. Omitted when unknown */
    @SerialName(value = "source_usd_price")
    val sourceUsdPrice: Double? = null,

    /* USD price of one whole destination-asset unit at quote time. Omitted when unknown */
    @SerialName(value = "destination_usd_price")
    val destinationUsdPrice: Double? = null,

    /* Aggregator-assigned highlights (e.g. fastest, cheapest) */
    @SerialName(value = "tags")
    val tags: List<@Contextual CrossSwapRouteTag>? = null,
    @SerialName(value = "warnings")
    val warnings: List<String>? = null,
    @SerialName(value = "fees")
    val fees: List<CrossSwapFee>? = null,

    /* Signing payloads for this route, prepared inline. Present only when the quote request had `include_payload: true` and the provider's prepare step succeeded. When present the client can sign and call `/v2/swap/payload/{payload_id}/submit` directly, skipping the separate `/v2/swap/route/{route_id}/prepare`.  */
    @SerialName(value = "payloads")
    val payloads: List<CrossSwapPayload>? = null
) {
}


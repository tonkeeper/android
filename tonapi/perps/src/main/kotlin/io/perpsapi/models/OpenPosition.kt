/**
 * AUTO-GENERATED FILE - DO NOT EDIT
 *
 * This file was automatically generated by the Tonapi OpenAPI Generator.
 * Generator: OpenAPI Generator (https://openapi-generator.tech)
 *
 * To regenerate, run: ./tools/scripts/generators/tonapi/update-all.sh
 * Manual changes will be overwritten on next generation.
 */

package io.perpsapi.models

import kotlinx.serialization.Serializable
import kotlinx.serialization.SerialName
import kotlinx.serialization.Contextual

@Serializable
data class OpenPosition(
    /* Identifies the position for the detail endpoint. Opaque -- read it from here and hand it back, do not parse it. A position has no id of its own on Lighter: it is keyed by account and market, and the account is the one the wallet proved, so this is composed here. It names the slot rather than the episode: closing and reopening the same market returns the same id. */
    @SerialName(value = "id")
    val id: String? = null,

    @Contextual @SerialName(value = "venue")
    val venue: Venue? = null,

    /* The venue's own market number. Redundant with part of `id`, and kept because the trading screen is addressed by it. */
    @SerialName(value = "market_index")
    val marketIndex: Int? = null,
    @SerialName(value = "symbol")
    val symbol: String? = null,

    @Contextual @SerialName(value = "side")
    val side: PositionSide? = null,

    /* Unsigned position size in the traded asset. */
    @SerialName(value = "size")
    val propertySize: String? = null,

    /* Notional value of the position in quote units. */
    @SerialName(value = "position_value")
    val positionValue: String? = null,
    @SerialName(value = "avg_entry_price")
    val avgEntryPrice: String? = null,

    /* From the markets snapshot, not from the position -- a position carries no price of its own. Empty when that snapshot is unavailable. */
    @SerialName(value = "mark_price")
    val markPrice: String? = null,

    /* Zero on a position with no leverage, which is a value rather than a gap. */
    @SerialName(value = "liquidation_price")
    val liquidationPrice: String? = null,

    /* How far the liquidation price sits from the mark price, in percentage points. Empty when either price is zero. */
    @SerialName(value = "liquidation_distance_pct")
    val liquidationDistancePct: String? = null,

    /* Margin backing the position, in quote units. Lighter allocates margin per position only in isolated mode; in cross mode it reports zero there, and this is derived from the notional and the initial margin fraction instead. */
    @SerialName(value = "margin")
    val margin: String? = null,

    /* Margin plus the unrealized result -- what the position is worth to the holder right now. Derived here so every client shows the same number. */
    @SerialName(value = "equity")
    val equity: String? = null,
    @SerialName(value = "unrealized_pnl")
    val unrealizedPnl: String? = null,

    /* Accumulated from partial closes on this position. */
    @SerialName(value = "realized_pnl")
    val realizedPnl: String? = null,

    /* The result so far against the margin invested, in percentage points. Funding is not in the numerator: Lighter has already folded it into the margin balance. Empty when there is no margin to divide by. */
    @SerialName(value = "roi_pct")
    val roiPct: String? = null,

    /* Derived from the initial margin fraction. Zero means it could not be derived, never that the position is unleveraged -- no leverage is 1. */
    @SerialName(value = "leverage")
    val leverage: Int? = null,

    @Contextual @SerialName(value = "margin_mode")
    val marginMode: MarginMode? = null,

    /* Funding accumulated over the life of the position. Negative means paid out. */
    @SerialName(value = "funding_paid")
    val fundingPaid: String? = null,

    /* Orders resting on this market, take-profit and stop-loss legs among them. Zero means the detail endpoint has no orders to add. */
    @SerialName(value = "open_order_count")
    val openOrderCount: Int? = null
) {
}


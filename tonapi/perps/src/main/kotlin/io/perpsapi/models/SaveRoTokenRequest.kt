/**
 * AUTO-GENERATED FILE - DO NOT EDIT
 *
 * This file was automatically generated by the Tonapi OpenAPI Generator.
 * Generator: OpenAPI Generator (https://openapi-generator.tech)
 *
 * To regenerate, run: ./tools/scripts/generators/tonapi/update-all.sh
 * Manual changes will be overwritten on next generation.
 */

package io.perpsapi.models

import kotlinx.serialization.Serializable
import kotlinx.serialization.SerialName
import kotlinx.serialization.Contextual

@Serializable
data class SaveRoTokenRequest(
    /* When the client says the token was issued. Recorded and forwarded to Argus, whose event schema carries the field; nothing here depends on it, because a read-only token states no issue time of its own and this cannot be checked. */
    @Contextual @SerialName(value = "issued_at")
    val issuedAt: java.time.OffsetDateTime,

    /* The wallet's consent to storing this token, signed with its own root key: hex of a 65-byte recoverable secp256k1 signature (R | S | V) over LP(\"perps.rotoken.store.v1\") | LP(wallet_id) | LP(l1_address) | LP(token_hash) | LP(name) | LP(issued_at), in the same length-prefixed framing custodial-battery uses for its send proof. token_hash is sha256 of the X-Lighter-Auth value in lowercase hex — the token is named by its hash, never signed as itself; name is what this body carries, empty string when omitted; issued_at is unix nanoseconds in decimal, because it is kept and forwarded at that precision; l1_address is lowercase with its 0x. Note that l1_address is named here and nowhere in the body. signature only proves that somebody holds the key to an address; this is the wallet saying which address is its own. Both are needed while the wallet has none recorded: without this one an EIP-712 signature captured anywhere could bind the wallet to a stranger's Lighter account. Required on every stored token, not only the first, and absent answers 401 operation_proof_required. Requires a format 2 wallet_id, which every multichain wallet has. */
    @SerialName(value = "proof")
    val proof: String,

    /* Optional client-supplied label for the token. */
    @SerialName(value = "name")
    val name: String? = null,

    /* Unix seconds past which the signature below is refused, capped by the service at ten minutes ahead. Travels with signature: one without the other is a 400, because a missing deadline would otherwise be reported as a signature that has expired. */
    @SerialName(value = "deadline")
    val deadline: Long? = null,

    /* EIP-712 signature over LinkWallet(string walletId,uint64 deadline), under domain {name: TonkeeperPerps, version: \"1\", chainId: 1} with no verifyingContract. Encoded as 65 bytes of r‖s‖v hex; v may be 27/28 or 0/1, and the high-s form of a signature is refused so one proof has one encoding. Read only while the wallet has proved no L1 address yet, which for most callers happened on their first deposit. Send it when you can: the client cannot tell when it is needed, and missing when it is answers 400 address_proof_required. */
    @SerialName(value = "signature")
    val signature: String? = null
) {
}

